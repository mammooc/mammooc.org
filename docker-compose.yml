db:
  image: postgres
  ports:
    - "5432"
redis:
  image: redis
  ports:
    - "6379"
web:
  build: .
  environment:
    - RAILS_ENV=production
    - RACK_ENV=production
    - SECRET_KEY_BASE=somesceretkebasehere4jklfnfrnjkrge
  command: foreman start
  volumes:
    - .:/myapp
  ports:
    - "3000:3000"
  links:
    - db
    - redis

h2
  = t('users.password.forgot')
= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f|
  /= devise_error_messages!
  .form-group
    .field
      = f.label :primary_email
      = f.email_field :primary_email, :class => 'form-control',  autofocus: true
  .actions
    = f.submit t('users.password.reset'), :class => 'btn btn-primary'
= render "devise/shared/links"

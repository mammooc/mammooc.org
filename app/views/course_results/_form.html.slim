= form_for(@course_result, :html => { :class => "form-horizontal", :role => "form" }) do |f|
  - if @course_result.errors.any?
    .alert.alert-danger.alert-dismissable role="alert"
      button.close type="button" data-dismiss="alert"
        span aria-hidden="true"
          | &times;
        span.sr-only
          | Close
      h4= "#{pluralize(@course_result.errors.count,"error")} prohibited this course_result from being saved:"
      ul
        - @course_result.errors.full_messages.each do |msg|
          li= msg
  .form-group
    = f.label :maximum_score, :class => "col-sm-2 control-label"
    .col-sm-10
      = f.text_field :maximum_score, :class => "form-control"
  .form-group
    = f.label :average_score, :class => "col-sm-2 control-label"
    .col-sm-10
      = f.text_field :average_score, :class => "form-control"
  .form-group
    = f.label :best_score, :class => "col-sm-2 control-label"
    .col-sm-10
      = f.text_field :best_score, :class => "form-control"
  .form-group
    .col-sm-offset-2.col-sm-10
      = f.submit :class => "btn btn-primary"

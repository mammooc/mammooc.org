= form_for(@mooc_provider, :html => { :class => "form-horizontal", :role => "form" }) do |f|
  - if @mooc_provider.errors.any?
    .alert.alert-danger.alert-dismissable role="alert"
      button.close type="button" data-dismiss="alert"
        span aria-hidden="true"
          | &times;
        span.sr-only
          | Close
      h4= "#{pluralize(@mooc_provider.errors.count,"error")} prohibited this mooc_provider from being saved:"
      ul
        - @mooc_provider.errors.full_messages.each do |msg|
          li= msg
  .form-group
    = f.label :logo_id, :class => "col-sm-2 control-label"
    .col-sm-10
      = f.text_field :logo_id, :class => "form-control"
  .form-group
    = f.label :name, :class => "col-sm-2 control-label"
    .col-sm-10
      = f.text_field :name, :class => "form-control"
  .form-group
    = f.label :url, :class => "col-sm-2 control-label"
    .col-sm-10
      = f.text_field :url, :class => "form-control"
  .form-group
    = f.label :description, :class => "col-sm-2 control-label"
    .col-sm-10
      = f.text_area :description, :class => "form-control"
  .form-group
    .col-sm-offset-2.col-sm-10
      = f.submit :class => "btn btn-primary"
